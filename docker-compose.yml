version: '3.8'

services:
  aurascript-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aurascript-platform
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
